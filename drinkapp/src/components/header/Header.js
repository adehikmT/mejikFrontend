import React from "react";
import { Header } from "react-native-elements";
import {Button} from "react-native-web"

function Head(props) {
  return (
    <>
      <Button/>
    </>
  );
}

export default Head;
